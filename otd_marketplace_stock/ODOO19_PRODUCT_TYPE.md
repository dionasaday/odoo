# 📦 Odoo 19 Product Type และ Track Inventory

## Product Type ใน Odoo 19

ใน Odoo 19, Product Type มี 3 ตัวเลือก:

1. **"Goods"** (สินค้า)
   - ตรงกับ `type='consu'` (consumable)
   - สามารถขายและซื้อได้
   - **สามารถ track inventory ได้** ถ้าเปิดใช้งาน

2. **"Service"** (บริการ)
   - ตรงกับ `type='service'`
   - สามารถขายและซื้อได้ แต่ไม่สามารถเก็บสต็อกได้
   - **ไม่สามารถ track inventory ได้**

3. **"Combo"** (ชุดสินค้า)
   - อาจเป็น `type='consu'` หรือ type อื่น
   - ใช้สำหรับสินค้าชุด

## Track Inventory? Checkbox

### สาเหตุที่ Checkbox ไม่ถูกเลือกอัตโนมัติ:

1. **Product Type = "Goods" (`type='consu'`)**:
   - สามารถ track inventory ได้
   - แต่ต้องเปิดใช้งานด้วยตนเองหรือตั้งค่า `tracking` field

2. **การตั้งค่า `tracking` field**:
   - `tracking='none'` → ไม่ track inventory
   - `tracking='lot'` → track inventory โดย lot
   - `tracking='serial'` → track inventory โดย serial number

3. **"Track Inventory?" checkbox**:
   - อาจจะต้องเปิดใช้งานด้วยตนเองใน UI
   - หรืออาจจะเปิดอัตโนมัติเมื่อ `tracking` ไม่ใช่ `'none'`

## วิธีแก้ไข

### วิธีที่ 1: เปิดใช้งานด้วยตนเอง (แนะนำ)

1. ไปที่ **Inventory > Products**
2. เลือกสินค้า
3. ไปที่ Tab **"General Information"**
4. เลือก **"Track Inventory?"** checkbox
5. **Save**

### วิธีที่ 2: ตั้งค่า `tracking='lot'` (อาจจะช่วยได้)

โค้ดของเราตั้ง `tracking='lot'` แล้ว แต่ checkbox อาจจะยังไม่ถูกเลือก

ลอง sync products อีกครั้งและตรวจสอบว่า:
- Product Type = "Goods" ✅
- `tracking='lot'` ✅
- "Track Inventory?" checkbox = ? (อาจจะต้องเปิดด้วยตนเอง)

## สรุป

- **Product Type = "Goods"**: ถูกต้องแล้ว (`type='consu'`)
- **Track Inventory?**: อาจจะต้องเปิดใช้งานด้วยตนเอง (ข้อจำกัดของ Odoo 19)
- **Stock Sync**: ยังทำงานได้ปกติแม้ไม่เปิด checkbox (ใช้ `stock.quant` โดยตรง)

