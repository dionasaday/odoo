# 🔧 วิธีแก้ไข Job เก่าที่มี Warehouse Code ไม่ถูกต้อง

## ปัญหา

Job เก่าที่สร้างก่อนแก้ไขโค้ดยังมี `warehouse_code: "MW"` ใน Payload ซึ่งไม่ถูกต้อง และจะทำให้เกิด "Access Denied" error

## วิธีแก้ไข

### วิธีที่ 1: ลบ Job เก่าและสร้างใหม่ (แนะนำ)

1. **ลบ Job เก่า:**
   - ไปที่ **Marketplace** > **Jobs**
   - ค้นหา Job ที่มี `warehouse_code: "MW"` ใน Payload
   - ลบ Job เหล่านั้น

2. **สร้าง Job ใหม่:**
   - ไปที่ **Marketplace** > **Accounts** > **Zortout**
   - กดปุ่ม **"นำเข้าข้อมูลสินค้า"** (สำหรับ Product Sync)
   - หรือกดปุ่ม **"ดึงสต็อกสินค้า"** (สำหรับ Stock Sync)
   - ระบบจะสร้าง Job ใหม่ด้วยโค้ดที่แก้ไขแล้ว (จะ auto-detect และไม่ส่ง warehouse_code ที่ไม่ถูกต้อง)

### วิธีที่ 2: แก้ไข Payload ของ Job เก่า

1. **แก้ไข Payload:**
   - ไปที่ **Marketplace** > **Jobs**
   - เปิด Job ที่มีปัญหา
   - คลิกปุ่ม **"Payload"**
   - แก้ไข `warehouse_code` จาก `"MW"` เป็น `null` หรือลบออก
   - **Save**

2. **Run Job:**
   - กดปุ่ม **"Run Now"**
   - Job จะทำงานด้วย Payload ที่แก้ไขแล้ว

### วิธีที่ 3: ใช้ Dead Letter (ถ้า Job Failed)

1. **Move to Dead Letter:**
   - ไปที่ **Marketplace** > **Jobs**
   - เปิด Job ที่ Failed
   - กดปุ่ม **"Move to Dead Letter"**
   - Job จะถูกย้ายไป Dead Letter และไม่ทำงานอีก

2. **สร้าง Job ใหม่:**
   - ไปที่ **Marketplace** > **Accounts** > **Zortout**
   - กดปุ่ม **"นำเข้าข้อมูลสินค้า"** หรือ **"ดึงสต็อกสินค้า"**
   - ระบบจะสร้าง Job ใหม่

## ตรวจสอบ Job ใหม่

หลังจากสร้าง Job ใหม่:

1. **ตรวจสอบ Payload:**
   - ไปที่ **Marketplace** > **Jobs**
   - เปิด Job ใหม่
   - ดู Payload:
     - ควรไม่มี `warehouse_code` หรือ
     - `warehouse_code` เป็น `null` หรือ
     - `warehouse_code` เป็น known code (`ODS001`, `ODS002`, `W0005`, `W0006`)

2. **Run Job:**
   - กดปุ่ม **"Run Now"**
   - Job ควรทำงานสำเร็จ

## สรุป

- **Job เก่า**: มี `warehouse_code: "MW"` → Access Denied
- **Job ใหม่**: จะ auto-detect และไม่ส่ง warehouse_code ที่ไม่ถูกต้อง → Success

**แนะนำ**: ลบ Job เก่าและสร้างใหม่ด้วยโค้ดที่แก้ไขแล้ว

