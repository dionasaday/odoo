<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Marketplace Account: Multi-company rule -->
        <record id="marketplace_account_company_rule" model="ir.rule">
            <field name="name">Marketplace Account: multi-company</field>
            <field name="model_id" ref="model_marketplace_account"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <!-- Marketplace Shop: Multi-company rule -->
        <record id="marketplace_shop_company_rule" model="ir.rule">
            <field name="name">Marketplace Shop: multi-company</field>
            <field name="model_id" ref="model_marketplace_shop"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <!-- Marketplace Product Binding: Multi-company rule -->
        <record id="marketplace_product_binding_company_rule" model="ir.rule">
            <field name="name">Marketplace Product Binding: multi-company</field>
            <field name="model_id" ref="model_marketplace_product_binding"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <!-- Marketplace Order: Multi-company rule -->
        <record id="marketplace_order_company_rule" model="ir.rule">
            <field name="name">Marketplace Order: multi-company</field>
            <field name="model_id" ref="model_marketplace_order"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <!-- Marketplace Sync Rule: Multi-company rule -->
        <record id="marketplace_sync_rule_company_rule" model="ir.rule">
            <field name="name">Marketplace Sync Rule: multi-company</field>
            <field name="model_id" ref="model_marketplace_sync_rule"/>
            <field name="domain_force">['|', ('account_id.company_id', '=', False), ('account_id.company_id', 'in', company_ids)]</field>
        </record>

        <!-- Marketplace Job: Multi-company rule -->
        <record id="marketplace_job_company_rule" model="ir.rule">
            <field name="name">Marketplace Job: multi-company</field>
            <field name="model_id" ref="model_marketplace_job"/>
            <field name="domain_force">['|', ('account_id.company_id', '=', False), ('account_id.company_id', 'in', company_ids)]</field>
        </record>
    </data>
</odoo>

